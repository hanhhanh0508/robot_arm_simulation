<launch>
  <!-- Load the MoveIt controller configuration -->
  <rosparam file="$(find ur5e_moveit_config)/config/controllers.yaml"/>
  
  <!-- Set the controller manager type -->
  <param name="moveit_controller_manager" 
         value="moveit_simple_controller_manager/MoveItSimpleControllerManager"/>
  
  <!-- Trajectory execution parameters -->
  <param name="trajectory_execution/execution_duration_monitoring" value="false"/>
  <param name="trajectory_execution/allowed_execution_duration_scaling" value="2.0"/>
  <param name="trajectory_execution/allowed_goal_duration_margin" value="1.0"/>
</launch>
